services:
  - type: web
    name: opposite-game
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --worker-class eventlet -w 1 -b 0.0.0.0:$PORT apps.server.run:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.6"
